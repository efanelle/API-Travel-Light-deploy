
// average emissions are in lbs (per hour and mile)
// these numbers are per passenger
const planeEmissions = {
  perHour: 330,
  perMile: 0.63
};

const planeSpeed = 528;
//miles per hour

module.exports = {
  planeEmissions: planeEmissions,
  planeSpeed:planeSpeed
}